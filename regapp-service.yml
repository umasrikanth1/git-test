apiVersion: v1
kind: Service
metadata:
  name: valaxy-service
  labels:
    app: regapp
spec:
  selector:
    app: regapp

  ports:
    - port: 8081 #this is an unnecessaary commit
      targetPort: 8080

  type: LoadBalancer
